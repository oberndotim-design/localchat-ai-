# LocalChat AI
Android client for the LocalChat AI server running locally on your PC.

The APK is built automatically by GitHub Actions. After a successful build, download the **LocalChatAI-APK** artifact from the Actions run.
